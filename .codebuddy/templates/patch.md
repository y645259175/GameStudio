---
patch_id: PATCH-${NNN}
target: ${RELATIVE_PATH}
priority: ${P0_P1_P2_P3}
status: open
discovered_in: ${PROJECT}_sprint-${N}
discovered_at: ${DATE}
discovered_by: ${AGENT_OR_USER}
merged_at: null
---

# PATCH-${NNN}: ${ONE_LINE_TITLE}

## 问题

${WHAT_WENT_WRONG}

## 当前行为

```
${ORIGINAL_TEXT_QUOTE}
```

## 建议改动

${PROPOSED_CHANGE}

## 验证

${HOW_TO_VERIFY}
