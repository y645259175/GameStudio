---
description: Project structure rule - four-layer architecture (.codebuddy/ capability / studio/ workspace / engine/ binaries / projects/ projects) with path constraints and engine/ gitignored.
alwaysApply: true
enabled: true
updatedAt: 2026-05-14T21:08:00.000Z
provider:
---

# project-structure · 项目结构规范

## 四层架构

```
<workspace-root>/
├── .codebuddy/       ← 能力层（skill / agent / hook / rule / template）
├── studio/           ← 工作室层（docs / reference / config）
├── engine/           ← 引擎层（引擎二进制，gitignored，不进版本控制）
│   └── Godot/        ← Godot_v4.6.2-stable_win64.exe
└── projects/         ← 项目层（<project-name>/ gdd/ stories/ adr/ ...）
```

### `.codebuddy/` 能力层

| 子目录 | 内容 |
|---|---|
| `skills/<name>/SKILL.md` | 技能定义 + 资源 |
| `agents/<name>/AGENT.md` | Agent 定义 |
| `hooks/<name>.sh` | Hook 脚本 |
| `rules/<name>/RULE.mdc` | 规则定义 |
| `templates/<name>.tpl` | 模板文件 |
| `plans/` | 迁移规划 + 日志 |

### `studio/` 工作室层

| 子目录 | 内容 |
|---|---|
| `docs/` | 工作室级文档（language-policy 等）|
| `docs/engine-reference/<engine>/` | 引擎参考（Phase 1 占位）|
| `reference/` | 只读参考区（Phase 1.5 归位 analysis-report/ my-game/）|

### `projects/` 项目层

每个项目 `projects/<name>/` 包含以下标准子目录（由 `new-project` skill 创建）：

| 子目录 | 内容 |
|---|---|
| `PROJECT.md` | 项目元数据（引擎/phase/里程碑）|
| `README.md` | 项目级快速开始 + smoke checklist |
| `gdd/` | GDD 文档（8 节结构）|
| `stories/` | User stories |
| `epics/` | Epics |
| `adr/` | Architecture Decision Records |
| `sprints/` | Sprint plan / smoke / retro（统一目录）|
| `data/` | 数值表（JSON，策划主本）|
| `reports/` | 一致性检查 / daily report |
| `retros/` | Retro 归档索引 |
| `releases/` | Release notes / checklist |
| `qa/` | 测试策略 / 回归矩阵 / run-tests.ps1 |
| `art/` | 参考图 / style guide / prompt 文件 |
| `assets/` | 美术资产（art-asset-pipeline 产出）|
| `docs/` | 项目级文档（README 之外的）|
| `game/` | **交付物**（复制出来即可独立运行）|

> `game/` 是自包含交付目录，不引用 `game/` 外面的文件。`data/` 是策划主本，`game/data/` 是程序副本（通过 `sync-data.ps1` 同步）。

## 路径约束（Phase 1 期间强制）

### 约束 1：不引用原路径

skill / agent / hook / rule / template 的正文**不得**引用 `analysis-report/` 或 `my-game/` 的原始路径。

### 约束 2：使用未来位置

需要引用上游内容时，使用 Phase 1.5 归位后的路径 `studio/reference/<name>/` 并标注 `[Phase 1.5+ TODO]`。

### 约束 3：占位显式标注

占位内容必须以 `[Phase 1.5+ TODO]` 或 `[Phase 2 TODO]` 显式标注，不得留空无说明。

## 关联 skill / agent

- `new-project` → 在 `projects/` 下创建项目骨架
- `setup-engine` → 创建引擎相关目录
- `consistency-check` → 验证路径约束
- `architect` agent → 架构决策影响目录结构时参考
