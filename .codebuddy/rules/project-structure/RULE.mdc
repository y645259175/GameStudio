---
description: Project structure rule (lite) - four-layer architecture overview with key constraints. For full directory specs see studio/docs/project-structure-full.md.
alwaysApply: true
enabled: true
updatedAt: 2026-05-19T10:59:00.000Z
provider:
---

# project-structure · 四层架构（精简版）

```
<workspace-root>/
├── .codebuddy/       ← 能力层（skill / agent / hook / rule / template）
├── studio/           ← 工作室层（docs / reference / config）
├── engine/           ← 引擎层（Godot 二进制，gitignored）
└── projects/         ← 项目层（每个项目一个子目录）
```

## 关键约束

1. **代码 / 游戏资产只放 `projects/<name>/game/`**（自包含交付目录）
2. **设计文档放 `projects/<name>/gdd/`**，story 放 `stories/`
3. **不要在 workspace 根目录创建文件**（除 README.md）
4. **`engine/` 不进 git**
5. **路径里不得引用 `analysis-report/` 或 `my-game/`**（历史遗留，已迁走）

## 强制前置读取

**以下操作执行前，必须先 `read_file studio/docs/project-structure-full.md`，读完再操作，否则禁止执行：**

- 新建项目（`new-project` skill 或手动创建 `projects/<name>/`）
- 在 `projects/` 下新建任何子目录
- 创建或移动文件到一个之前不存在的路径
- 调用 `setup-engine` skill
- 修改项目骨架结构（增删顶层目录）
- 编写 backlog 条目（需要知道格式约定）

**不需要读取的场景**：在已有目录下编辑/新