---
description: GDD authoring standard - 8 mandatory sections, content precision rules (animation/perception/numbers), review gates, and anchor stability for cross-doc referencing.
alwaysApply: false
enabled: true
updatedAt: 2026-05-20T15:42:00.000Z
provider:
---

<!-- OVER_LIMIT_REASON: 8 节硬性结构 + 内容精度规则 + 锚点稳定 三类规则互相引用，
designer agent 起草 GDD 时一次性需要全部上下文。拆 MANUAL 会让 designer 反复跳转。
本 rule 不是 alwaysApply，仅 GDD 任务时按需触发，注入成本可接受。 -->

# design-authoring · 设计文档起草规范

## GDD 内容精度三原则

策划案是"照着做就能做对"的规格说明书，但**不越界代替其他角色的决策**。

### 原则 1 · 动画/动效

GDD 必须写清楚：
- ✅ **触发时机**（"砖块 HP 归零时"）
- ✅ **表现描述**（"同色碎片四散 + 淡出"）
- ✅ **可选的时长范围 / 频率 / 颜色倾向**（"约 0.5-0.8s"、"偏暖色"）

GDD 不写（由其他角色出）：
- ❌ 具体像素尺寸 / 精确缓动曲线 / 最终配色值 → `ux-designer` 出交互规格
- ❌ 美术风格 / 资产细节 → `art-director` 定方向

### 原则 2 · 道具/技能/系统的玩家感知

每个系统/道具/技能必须覆盖**玩家感知全链路**：

| 时刻 | 必须描述 |
|---|---|
| 看到（出现） | 玩家如何识别它是什么 |
| 获得（触发） | 获得瞬间发生什么反馈 |
| 生效中 | 玩家如何知道效果还在 |
| 结束 | 效果消失时的表现 |

### 原则 3 · 数值

- ❌ 不在 GDD 正文中写死"最终数值"或使用"适当"/"合理"等模糊词
- ✅ 在 GDD 中标注数值表的字段名 + 填一个**基准初始值**作为参考
- ✅ 实际数值独立管理在 `data/*.json`（或 TOML/CSV），程序读表获取
- ✅ 调数值时**只改数据文件**，不需要改代码或 GDD

示例：
```
球速：见 `data/levels.json` → levels.<N>.ball_speed（基准：L1=300 px/s）
```

## 角色分工

| 角色 | 职责 | agent |
|---|---|---|
| 游戏设计师 | GDD 内容主笔（功能 + 玩家感知 + 数值表结构） | `designer` |
| 交互设计师 | 出交互/动效规格（时长 / 缓动 / 反馈通道） | `ux-designer` |
| 美术总监 | 定视觉方向 + 审核最终效果 | `art-director` |
| 制作人 | 拍板 GDD 进入下一阶段 | `producer` |

## 8 节强制结构

GDD 文件**必须**包含以下 8 个二级标题，顺序固定：

| # | 章节 | 内容 |
|---|---|---|
| 1 | `## 概述` | 一句话核心概念 + 目标玩家 + 类型定位 |
| 2 | `## 玩法循环` | core loop / meta loop |
| 3 | `## 视觉与美术` | 风格基线 / 色板 / 参考图引用 |
| 4 | `## 系统设计` | 战斗 / 经济 / 养成 / 社交 等子系统拆解 |
| 5 | `## 数值与平衡` | 数值表结构 / 关键公式 / 平衡假设 |
| 6 | `## 内容与节奏` | 关卡 / 章节 / 故事 / 进度曲线 |
| 7 | `## UX 与 HUD` | 界面流转 / HUD 元素清单 / 无障碍 |
| 8 | `## 交付与验收` | 验收标准 / 风险 / 已知限制 |

## 锚点稳定性原则

- 章节标题**不得修改**（标点 / 措辞 / 大小写都不行）
- 章节顺序**不得调整**
- 章节内子标题（`### xxx`）可自由调整
- 新增第 9 节及以上必须经 ADR 决议并同步更新本规则

## frontmatter 规范

```yaml
---
gdd_id: <项目缩写>-<章节号>-<语义名>
status: draft / review / locked
last_review: YYYY-MM-DD
sections_complete: [1, 2, 3, 4, 5, 6, 7, 8]
---
```

`sections_complete` 用于 `review-all-gdds` 跨章节扫描定位空节。

## review 门槛

| 状态转换 | 触发条件 |
|---|---|
| draft → review | 8 节均有非空内容 |
| review → locked | `design-review` skill 通过 + producer 拍板 |
| locked → draft | 必须经 ADR 决议 |

## 锚点引用约定

story / ADR 引用 GDD 时使用：

```
GDD §<章节号>.<子标题路径>（gdd-<id>.md）
例：GDD §4.战斗.伤害公式（gdd-combat.md）
```

## 关联 skill / agent

- `design-review` 起草 / 审校
- `review-all-gdds` 跨章节扫描
- `quick-design` 小型设计决策（不入 GDD 主体）
- `consistency-check` 引用闭合验证
- `designer` agent 主笔 / `ux-designer` 出交互规格 / `art-director` 出视觉方向 / `producer` 拍板
