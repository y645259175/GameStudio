---
description: 任何 agent spawn sub-agent 时必须遵守的契约。包含 5 条硬性契约 + spawn 模板入口。详细见 MANUAL.md。
alwaysApply: false
enabled: true
updatedAt: 2026-05-20T15:30:00.000Z
---

# Agent Spawn Contract · CORE

## 5 条硬性契约（spawn 前必检）

1. **现状注入**：spawn prompt 必须含 read_file 指令读 PROJECT.md + 当前 backlog + 自身 AGENT.md
2. **任务模式声明**：DRAFT / REFINE / PATCH / REVIEW 四选一，明确写在 prompt 头
3. **交付-关闭顺序**：sub-agent 必须先 send_message 交付内容，再 shutdown_response
4. **落盘强制**：产出 ≥ 50 行的内容必须落盘到指定路径，不能只 inline 返回
5. **文件操作必须用 IDE 工具**：禁止 Remove-Item / rm / del 命令行

## 何时升级到 MANUAL

需详细 SOP / 模板 / 自检清单时 read_file `MANUAL.md`：
- spawn 9 个 TPL 模板（实现 / milestone gate / GDD 起草 / bug 排查 / 资产评审 / 一致性审 / 测试编写 / commit review / vertical slice 5 项清单）→ §TPL-XX
- 知识注入协议（产出契约 + self_rubric）→ §知识注入
- 并行模式选择（task batch vs team mode）→ §并行模式
- 防截断协议 / shadow spawn 参数 / 项目上下文自动注入 → §专项协议

## 历史溯源

详细判例 / combo-A/B 演化过程 → 见 `ARCHIVE.md`
