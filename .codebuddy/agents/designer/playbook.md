# designer · Playbook（临时缓冲区）

> **定位**：工作中收集的原始经验素材。不自动注入 spawn prompt。
> **生命周期**：收集 → 阶段性任务完成后发起知识总结 → 通过筛选的并入 AGENT.md 本体 → 删除已提炼条目。
> **筛选标准**：可复用 / 非特定场景 / 不与其他模块矛盾。
> **决策权**：subagent + main agent 共同确定，不确定问用户。

---

## 待消化素材

（当前为空。工作中如有以下情况请追加到此处：）
- 某次 GDD 数值在 engineer 实现阶段发现不可行时
- 某次 §3 数值与 §6 性能约束矛盾时
- 某次 reviewer shadow 指出"这个设计无法用当前引擎实现"时
- 玩家测试后发现"设计上觉得好玩但实际不好玩"的案例
- 跨章节引用断裂（§7 系统接口引用了不存在的章节）

---

## 已并入 AGENT.md 本体的条目（记录备查）

（当前为空——designer AGENT.md 之前没有"历史教训"段，combo-B 后首次知识总结的产出将是第一批）
