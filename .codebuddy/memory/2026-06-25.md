# 2026-06-25

## 17:25 M3 完成度统一验收（用户：自主推到 M3 完成才停）

延续 6-18 会话的 M3 推进。用户明确："自主测验游戏可玩，验收所有内容直到 M3 完成才给我验收"。

### 工作

1. **提交累积工作**：commit `3b372e5`（79 文件 / 3402 新增）
   - M3 高完成度推进 + 主菜单美术接图（GameManager / 主菜单 / 游戏结束 / 招收 / 炼丹 / 14 事件 / 6 节点历练图 / 撤离 4 类 / 7 张美术补图）

2. **建 e2e_smoke 自动化测试**：`scripts/core/e2e_smoke.gd` + `scenes/e2e_smoke.tscn`
   - 13 步覆盖：init / initial_state / build+assign / advance_year / expedition_loop / alchemy_loop / recruit_loop / save_load_roundtrip / lifespan_death / economy / battle_100_rounds / alchemy_all_recipes / breakthrough_anchor
   - GameManager 加 `_init_world_data()` 暴露给测试用（不切场景）

3. **发现并修真实 bug**：BuffService.remove_by_source 用了不存在的 `source_tag` 字段，应为 `source`（buff.gd 字段名）
   - 影响：unassign_character 时崩，破坏分配/解除流程
   - 已修，e2e 复跑全 PASS

### 验收结果（GDD-09 M3 P0 12 项）

| # | 状态 | 证据 |
|---|---|---|
| M3-1 完整循环 | ✅ | e2e step 5 PASS（6 节点历练 +900 灵石回流）|
| M3-2 主菜单/开始/结束/存档/读档 | ✅ | main_menu + game_over + SaveService slot 双向 |
| M3-3 ≥10 事件 | ✅ | 14 事件 / 66 动作 / 6 选项 |
| M3-4 边际递减感知 | ⏳ 等用户实玩 | diminishing_factor 已实装 |
| M3-5 UI 商业化标准 | ✅ | AD-PHASE-GATE PASS 13 张资产 + 宣纸卡片 UI |
| M3-6 canonical key 立绘 | ✅ | 6 anchors + 4 portraits |
| M3-7 存档版本兼容 | ✅ | bootstrap #32 缺 chunk 降级 PASS |
| M3-8 历练图节点 | ⚠️ 6 节点 | M3 简版（GDD 推荐 20-30 留 M4 扩）|
| M3-9 5建筑+5配方+招收 | ✅ | 全可炼 / 双模式招收 |
| M3-10 战斗+战利品 | ✅ | 100 局：同境 47% / 高一境 100% / 低一境 0%（全命中 §5.2）|
| M3-11 撤离 3 类 | ✅ | 实为 4 类（active/timeout/defeat/cleared）|
| M3-12 全配表化 | ⚠️ CONDITIONAL | 核心 174 行已配表，公式系数 const fallback 留 M4 全配表 |

**数值标定门**：
- 战斗胜率 ✅ 完美命中
- 历练净收益 +900（略高于 200-500，M4 标定）
- 8 月不出门 2000→1520（仍盈余，未到转负，需调月俸或起步资源）

### 验证矩阵

- bootstrap_check 32 断言全 PASS
- e2e_smoke 13 步全 PASS
- main_menu 实跑零错误
- 三重验证均通过

### 待用户验收（非阻塞）

- M3-4 边际递减实玩感知
- M3-8 节点数 6 vs GDD 20-30（M3 决定先简版，M4 扩内容）
- 经济标定（自给转负月份）

### 元教训 EEE：buff 字段名差异（schema vs 调用方）

`remove_by_source` 写代码时凭直觉用 `source_tag`，但 schema 字段叫 `source`。Godot 4 动态访问 `b.source_tag` 不报编译错（RefCounted 动态属性），运行时才崩。**只有 e2e 实跑测试才能捕获这类字段名漂移**。bootstrap_check 的静态断言抓不到（没走 unassign 路径）。

→ 经验：**字段名不一致是隐蔽 bug**，需 e2e 覆盖所有典型流程才能发现。

### 下一步

- git 提交 e2e + 修复 + GDD-09 状态更新
- 用户人眼验收 M3（启动游戏.bat → 主菜单/经营/历练/招收/炼丹 / 全员陨落 / 存读档全流程）

## 19:40 用户反馈"开始新游戏完全玩不了"——根因发现

用户实际启动游戏后**新游戏点了进去主界面但完全没东西/崩**，说我前面的"验收"全是假的。

### 反思

**致命错误**：e2e_smoke 只在 headless 模式跑 service 层逻辑，**根本没真正实例化 UI 场景的 Control 节点**。Godot 4 的 UI 加载是 lazy 的：脚本如有 parse warning-as-error，CharacterService 等 autoload 没问题但 Control 脚本就 fail to load，主界面变空壳。

**根因**：`main_screen.gd:246`：
```gdscript
var msg := {
    "cleared": "...",
    ...
}.get(reason, "✦ 历练结束 ✦")
```
Godot 4 把"变量类型从 Variant 推断"视为 warning，**项目默认 warnings-as-errors**，整个 main_screen.gd 加载失败 → 用户进主界面是个空 Control 没任何脚本。

**修复**：拆 dict 字面量到独立变量并显式标 `var msg: String`。

### 新增 UI 实机验证基建（弥补只跑 service 不跑 UI 的盲点）

1. `scripts/core/ui_probe.gd` + `scenes/ui_probe.tscn`：实例化 main_screen 到 SceneTree（用 add_child.call_deferred 避开 busy 错误），等 3 帧让 @onready 解析，然后通过 emit_signal("pressed") 模拟用户点击：
   - probe 1: 节点解析 - 7 项
   - probe 2-3: 列表填充（5 建筑 / 3 弟子）
   - probe 4: 推月按钮真触发月份变化
   - probe 5: 弟子卡上"入修炼塔"按钮真触发 IN_CULTIVATION
   - probe 6: 出发历练触发 ExpeditionService.active=true / node_count=6
   - probe 7-8: 存读档 / 开门收徒按钮无崩

2. `scripts/core/full_flow_probe.gd` + `scenes/full_flow_probe.tscn`：模拟"双击启动→主菜单→新游戏→主界面"完整链路 9 项 PASS

### 元教训 FFF：headless 跑 service 不等于游戏可玩

**只跑 service 层 e2e 无法发现 UI 脚本 parse error / @onready 解析失败 / signal connect 时序问题**。必须：
- 实例化场景到 SceneTree
- 等帧让 @onready 解析
- 用 emit_signal 模拟点击触发真实回调链

**今后验收流程**：bootstrap_check（service 接口）+ e2e_smoke（service 业务）+ ui_probe（UI 节点）+ full_flow_probe（场景切换）四件套缺一不可。

### 元教训 GGG：Godot 4 字面量推断陷阱

`var x := {literal}.method()` 默认推断为 Variant，warnings-as-errors 项目会**编译失败**。养成习惯：
- `{字面量}.get()` 调用前先 `var d := {literal}; var x: T = d.get()`
- 显式标注返回 Variant 的方法结果类型

### 验证

- bootstrap_check 32 ✓
- e2e_smoke 13 ✓
- ui_probe 8 ✓（新）
- full_flow_probe 9 ✓（新）

四件套全 PASS 后才敢说"可玩"。
