# 2026-05-22 工作笔记

## 14:39 用户反馈"对话窗口消失"

确认昨天 D 系列重构 + 工作区遗留入库都已 commit 落地（`b148cfc / 97cf8cd / c956e26`），工作区干净。但**健康体检结论**只在对话回复里，没落地任何文件——这是 AP-06 同类（关键洞察没沉淀）。

## 14:41 重跑健康体检

跑 `.codebuddy/temp/health_check.py` 拿到客观数据，结论：
- **真在工作**：4 个挂载 hook（含 PreToolUse 860 行实战日志）+ 5 核心 agent 25 个 calibration + commit-msg lint
- **自欺**：18/25 skill 纯文档（可执行率 12%）+ 26/31 agent 没三层化 + **零 agent/skill 调用日志**

## 15:27 落地后台任务 + P0 启动

- 写 `studio/reports/system-health-2026-05-22.md`（体检完整报告 + 推荐行动 + backlog 调整建议）
- 建 `.codebuddy/memory/MEMORY.md`（首次建立长期记忆文件）+ 今日 daily（本文件）
- 接下来做 P0-A（启用 log-agent hook）+ P0-B（5 个纯文档 skill 加 run.py）

## 15:38 P0-A + P0-B 完成（commit 619612e）

### P0-A 度量基础设施
- 增强 `pre-tool-bash.py`：Task → `agent-spawn-*.jsonl` · skill run.py → `skill-call-*.jsonl`
- 新建 `.codebuddy/scripts/agent_metrics.py`：扫 jsonl 给统计
- **从此能用数据回答"系统真在工作吗"**

### P0-B 5 个 skill 加 run.py（可执行率 12% → 32%）
- 共享 `skill_lib.py`（frontmatter 解析 / story 加载 / GDD 8 节检查）
- consistency-check / story-readiness / smoke-check / quick-fix / story-done 全部可执行
- platformer-2 实测发现真问题（GDD 只 3/8 节、所有 story NOT-READY）
- metrics 今日捕获 8 次 skill 调用 ✓

## 教训沉淀（短期）

- **关键洞察必须落到文件**：哪怕只是 reply，重要数据 / 结论也要写到 `studio/reports/` 或 `studio/docs/`
- **memory 文件之前一直没建**：违反了 `<working_memory_files>` 规则。今天补上
- **度量先于优化**：D 系列重构投入 13h+ 但没有度量，相当于盲飞。P0-A 之后未来所有改进都有反馈环
- **AI 估时校准**（更新到 ID 59617001）：本次 P0-A + P0-B 实际 ~50 分钟（写 5 run.py + lib + metrics），之前估 4-5h，偏差 5-6 倍。再次印证"模板化任务对 AI 极快"。

## 15:48 清理 `_pipeline_stage/` 遗留目录

- 该目录含 25 张 png（5 资产 × 5 阶段：raw/remove_bg/crop/shrink/quantize），是之前跑 `art-asset-pipeline` + `timiai-image` 后处理的临时输出
- commit `97cf8cd` 清工作区时一并入库，但违反"不在根目录创建文件"规则，且代码库无任何引用
- 用户选择 A（直接删除），25 文件 + 空目录已清除
- **教训**：管线临时产物应输出到 `.codebuddy/temp/` 或项目内，不应放 workspace 根目录
